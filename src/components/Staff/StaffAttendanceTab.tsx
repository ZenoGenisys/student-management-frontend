import React from 'react';
import { Box, Typography } from '@mui/material';

const StaffAttendanceTab: React.FC = () => {
  return (
    <Box>
      <Typography variant="h5">Attendance Content</Typography>
    </Box>
  );
};

export default StaffAttendanceTab;
