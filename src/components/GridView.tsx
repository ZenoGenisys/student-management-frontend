import React from 'react';
import { Box, Typography } from '@mui/material';

const GridView: React.FC = () => {
  return (
    <Box>
      <Typography variant="h6">Grid View Content</Typography>
      {/* Add grid-specific content here */}
    </Box>
  );
};

export default GridView;