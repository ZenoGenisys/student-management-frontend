import React from 'react';
import { Box, Typography } from '@mui/material';

const ListView: React.FC = () => {
  return (
    <Box>
      <Typography variant="h6">List View Content</Typography>
      {/* Add list-specific content here */}
    </Box>
  );
};

export default ListView;